export * from './types/entry'
import FishDesign from './types/entry'
export default FishDesign

declare module "vue" {
  export interface GlobalComponents {
    {{#each components}}
      {{name}}: typeof import("./types/entry").{{component}},
    {{/each}}
  }
}
