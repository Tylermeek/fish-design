{
  "name": "fish-design-vite",
  "version": "1.0.4",
  "main": "index.js",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "prepare": "husky install",
    "lint": "eslint --fix --ext .ts,.vue packages"
  },
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@commitlint/cli": "17.0.2",
    "@commitlint/config-conventional": "17.0.2",
    "husky": "^8.0.3",
    "vite": "^3.2.5"
  },
  "devDependencies": {
    "@types/node": "^20.2.5"
  }
}
